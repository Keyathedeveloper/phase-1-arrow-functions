const divide = function(dividend, divisor) {
    return  dividend / divisor;
};

const result = divide(2000, 100);
console.log(result);

const square = (num) => {
    return num **2;
};

const add = (num1, num2) => {
    return num1 + num2;
}
